/*
const teenipings = [
{
    name: "떠벌핑",
    season: 1,
    img: "teenieping/1기/Tweetping.png",
    desc: "행복을 담당하는 티니핑 ✨"
  }
];

const container = document.getElementById("cardContainer");

teenipings.forEach(t => {
  container.innerHTML += `
    <div class="card">
      <img src="${t.img}">
      <h2>${t.name}</h2>
      <p>${t.desc}</p>
    </div>
  `;
});
*/
